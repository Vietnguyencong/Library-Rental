import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Welcome to the administration" />
        <CardContent>Building a library database. Our main goal is to get the maximum points and to destroy the other teams!!</CardContent>
    </Card>
);